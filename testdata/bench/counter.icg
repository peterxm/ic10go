// Testbench counter: reads its own LED Setting and increments it once per tick.
// Used to verify push / pause / set / step / state end to end on a real chip.
func main() {
	for {
		yield()
		d0.Setting = d0.Setting + 1
	}
}
