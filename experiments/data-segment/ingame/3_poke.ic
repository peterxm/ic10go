# 持久栈数据段 · 真机测试 3/6：poke 写本地栈
# d0 = LED Display（显示在 Setting）
#
# 用途：验证 poke（本地栈）写入的内容，能否被 get db 读到（即本地栈与 db 栈是否同一块）。
# 步骤：贴入本段 -> 运行，LED 显示 777。
#       然后贴入 4_get.ic 覆盖本段。

poke 50 12345
s d0 Setting 777
